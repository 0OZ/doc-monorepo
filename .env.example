# Database
DATABASE_URL=postgres://postgres:password@localhost:5432/doc_proxy

# Backend (doc-proxy)
PORT=3000
HOST=0.0.0.0
MOCK_MODE=true                    # Set to false to use real core server
CORE_API_URL=http://localhost:8081
CORE_API_TOKEN=dev-service-token

# Frontend (doc-web-view)
NEXT_PUBLIC_API_URL=http://localhost:3001
